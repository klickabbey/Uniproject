function calculateSum(a, b) {
  var res = a + b;
  console.log("The sum of the numbers is: " + res);
  console.log("The sum of the numbers is: " + parseInt(a + b));
  return res;
}

calculateSum(5, 10);

var number1 = typeof number1 === "number" ? number1 : 0;
